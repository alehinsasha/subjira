
&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	
	ДанныеСтроки = Элементы.Список.ТекущиеДанные;
	Если ДанныеСтроки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(Элементы.Список.ТекущаяСтрока) = Тип("СтрокаГруппировкиДинамическогоСписка") Тогда
		// TODO: использовать такую проверку в форме работы с задачами
	Иначе
		Описание = ОбщегоНазначенияВызовСервера.ДанныеОписания(ДанныеСтроки.Ссылка).Текст;
	КонецЕсли;
	
КонецПроцедуры
